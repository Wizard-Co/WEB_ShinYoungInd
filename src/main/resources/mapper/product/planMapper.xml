<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="wizard.ShinYoungInd.product.planInput.PlanMapper">

    <select id="getPlanList" statementType="CALLABLE"
            resultType="wizard.ShinYoungInd.product.planInput.DTO.Plan"
            parameterType="map"> {
        call xp_PlanInput_sOrder(
                #{chkDate, javaType=int, jdbcType=SMALLINT, mode=IN}
            , #{sDate, javaType=String, jdbcType=CHAR, mode=IN}
            , #{eDate, javaType=String, jdbcType=CHAR, mode=IN}
            , #{chkCustom, javaType=int, jdbcType=SMALLINT, mode=IN}
            , #{customID, javaType=String, jdbcType=VARCHAR, mode=IN}
            , #{chkArticle, javaType=int, jdbcType=SMALLINT, mode=IN}
            , #{articleID, javaType=String, jdbcType=VARCHAR, mode=IN}
            , #{chkBuyerArticleNo, javaType=int, jdbcType=SMALLINT, mode=IN}
            , #{buyerArticleNo, javaType=String, jdbcType=VARCHAR, mode=IN}
            , #{chkOrderNo, javaType=int, jdbcType=SMALLINT, mode=IN}
            , #{orderNo, javaType=String, jdbcType=VARCHAR, mode=IN}
            , #{chkComplete, javaType=int, jdbcType=SMALLINT, mode=IN}
            , #{chkClose, javaType=int, jdbcType=SMALLINT, mode=IN}
             )}
    </select>

    <select id="getPatternByArticleTypeID" statementType="CALLABLE"
            resultType="wizard.ShinYoungInd.product.planInput.DTO.PatternArticle"
            parameterType="String"> {
        call xp_PlanInput_sPatternByArticleTypeID(
                #{articleTypeID, javaType=String, jdbcType=VARCHAR, mode=IN}
             )}
    </select>

    <select id="getPatternDetail" statementType="CALLABLE"
            resultType="wizard.ShinYoungInd.product.planInput.DTO.PatternArticle"
            parameterType="map"> {
        call xp_PlanInput_sPatternArticleOne(
                #{patternID, javaType=String, jdbcType=VARCHAR, mode=IN},
                #{articleID, javaType=String, jdbcType=VARCHAR, mode=IN},
                #{outMessage, javaType=String, jdbcType=VARCHAR, mode=OUT}
             )}
    </select>


</mapper>